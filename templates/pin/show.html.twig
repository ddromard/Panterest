{% extends 'base.html.twig' %}

{% block title %}Pins Page
{% endblock %}

{% block body %}

	<article>
		<h1>{{ pins.title }}</h1>
		<p>{{ pins.description }}</p>
	</article>

    <a href="{{ path('app_home')}}">Go Back</a>
    
{% endblock %}
